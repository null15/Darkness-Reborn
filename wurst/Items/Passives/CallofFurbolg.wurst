package CallofFurbolg

import MyItemIds
import ClosureEvents
init
    callofFurbolg()
function callofFurbolg()
    EventListener.add(EVENT_PLAYER_UNIT_DEATH) ->
        if GetKillingUnit().hasItemById(ITEM_689) and GetRandomInt(0, 100) <= 100
            GetKillingUnit().addInt(1)