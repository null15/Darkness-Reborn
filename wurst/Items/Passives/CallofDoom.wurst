package CallofDoom

import MyItemIds
import ClosureEvents
init
    callofDoom()
function callofDoom()
    EventListener.add(EVENT_PLAYER_UNIT_DEATH) ->
        if GetKillingUnit().hasItemById(ITEM_690) and GetRandomInt(0, 100) <= 100
            GetKillingUnit().addAgi(1)