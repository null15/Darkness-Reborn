package Oclock

import ClosureEvents
import MyAbilityIds

init
    oClock()
    oooClock()

function oClock()
    EventListener.onCast('A0CL') caster ->
        caster.resetCooldown()

function oooClock()
    EventListener.onCast(ITEM_OOO_CLOCK_ABI1) (unit caster) ->
        caster.resetCooldown()